# Local setup

## Setup GMAIL API

- Create a [Google Cloud Project](https://console.cloud.google.com/projectcreate) (or use an existing one)
- Enable the GMAIL API under `API & Services > Enabled APIs & services`
- Setup Google Auth Platform
- Create OAuth 2.0 client with Desktop type
- Download credentials file
- Add yourself as test user in `Audience`
